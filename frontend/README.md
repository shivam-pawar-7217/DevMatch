# DevMatch Frontend

This is the UI for the DevMatch project. Built with React and Vite.

## Setup
Make sure you have Node installed. 

```bash
npm install
npm run dev
```

Note: It expects the FastAPI backend to be running on port 8000. If you're running this through the main docker-compose file, you don't need to do this manually.
